# ── Secrets ─────────────────────────────────────────────────
.env
.env.backup
.env.production
/auth.json
/storage/*.key

# ── Dependencies & build output ─────────────────────────────
/node_modules
/vendor
/public/build
/public/hot
/public/storage
/public/fonts-manifest.dev.json

# ── Local machine config ────────────────────────────────────
# Generated by setup-php-ini.ps1; hard-codes an absolute path to the local
# PHP install, so it is per-machine rather than per-project.
/php.ini

# ── Caches & logs ───────────────────────────────────────────
*.log
/.phpunit.cache
.phpunit.result.cache
/storage/pail

# ── Editors & tooling ───────────────────────────────────────
/.codex
/.cursor/
/.idea
/.nova
/.vscode
/.zed
/.phpactor.json
_ide_helper.php
Homestead.json
Homestead.yaml

# ── OS ──────────────────────────────────────────────────────
.DS_Store
Thumbs.db
